# Unresolved Backlog

The active checkpoint is in [planning/NOW.md](planning/NOW.md). This file
contains inactive unresolved outcomes only.

## Product

- Add `float16` import, storage, rendering, and analysis only through a
  separately approved dtype capability plan.
- Support mixed-dtype channel publication only through a separately approved
  physical package-layout plan.
- Consider filename-order preview or manual sequence ordering only after a
  concrete user need.
- Add rendered-viewport statistics only through an explicit scientific
  semantics design.
- Add project maintenance and Purge UI with a bounded, recoverable deletion
  contract.
- Reconsider smooth-linear performance or product exposure only through a
  separate product decision.

## Future Scope

- Publish full microscopy datasets only through a separately approved effort
  that resolves rights, privacy, hosting, citation, provenance, integrity,
  and validation.
- Reconsider segmentation only through a separately approved capability plan.
- Consider Windows, macOS, 4K, or broader hardware claims only after a
  concrete need and qualification scope.
- Consider external identity trust beyond package self-consistency only with
  an explicit trust and provenance model.
