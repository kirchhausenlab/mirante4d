# Current Work

Last updated: 2026-07-26

## Current Status

The foundation refactor through WP-15, import/preprocessing cutover, and
corrective `uint8` sentinel restoration remain implemented. Their scientific,
source-safety, persistence, bounded-resource, and product-boundary guarantees
remain in force.

The development-simplification and viewer-recovery program authorized on
2026-07-26 is complete. It superseded EP-00 through EP-07, preserved the strong
product boundaries, removed disproportionate qualification/provenance
machinery, recovered the unpublished viewer work, and delivered three
product-only rendering slices. The completed plan is retained in Git history;
implemented facts live in [current state](../CURRENT_STATE.md).

EP-00 and EP-01 evidence protocols remain retired. Their command, schema,
selection, receipt, replay, harness, shared gate/counter, and repeated
resource-union hashing surfaces are deleted. Normal product scripting, hard
resource caps, source/import/project workflows, screenshots, asynchronous GPU
timing, and independent numerical oracles remain.

## Active Checkpoint

No corrective viewer slice is active. R0–R4, V1–V3, and the compact terminal-
failure closeout are complete. Further work returns to ordinary
product-directed checkpoints and the [backlog](../BACKLOG.md).

The damaged linked viewer worktree is repaired and clean at reconstructed
commit `3dfe9de`. Both live recovery refs resolve to it. Strict `git fsck`,
reachable-object checks, index checks, and byte comparison with the independent
clone pass; corrupt metadata is retained only in the external recovery area.

Routine verification now has five public leaves, no empty doctest phase, and
does not compile test targets twice through Clippy. Six exhaustive or redundant
cases remain explicit developer-local checks. The first revised Rust run passed
all 1,357 routine cases in 81.9 seconds after a 35.4-second Clippy phase; the
six deep cases also passed independently.

The qualification-deletion cuts removed more than 43,000 lines. Resident wheel
and drag input now bypasses durable reducer/history/demand reconstruction,
renders only from a geometrically valid fully resident body, and commits once
after the gesture settles. Stale previews are retired across canonical view,
project, source, and tool transitions.

Every visible layer selects LOD independently from its affine cell footprint
in physical pixels for the actual 3D or cross-section view. Reuse requires that
projected map to match the installed current or staged target. Focused tests,
the independent LOD oracle, full app tests, compilation, Clippy, and independent
review passed. The normal release app also passed the real-display
resident-navigation, compound-camera, and 103-command render-mode/four-panel
workflows. Warm resident movement caused no reads, decodes, dataset requests,
or uploads and idle caused no queue submissions.

Cold refinement keeps the previous complete frame exact while an inactive
target streams, then swaps only after the replacement frame, extent,
requirements, and GPU coverage are exact. A real-display cold run on the
four-scale public import fixture showed hidden coverage at 20/100 while the
visible frame remained Exact, zero partial current frames, and one final
Complete/Current replacement without a fault.

DVR opacity now integrates physical world distance, and exact ISO pick
completeness includes the six-sample gradient halo. Independent Vulkan
reference checks passed. Two small shader candidates were measured and deleted:
neither met the predeclared five-percent retention threshold.

Native SIGINT/SIGTERM handling requests one prompt-free close and leaves the
existing joined shutdown path in sole control. The WGPU runtime latches the
first typed device-loss, out-of-memory, internal, or validation failure and
rejects later GPU work without selecting a fallback or recovery epoch.

The trusted Vulkan lane reports the focused tests directly. Ordinary GPU test
source is no longer hash-bound as a fixture, and a legacy receipt parser can no
longer turn passing GPU tests into a failed result because an internal
diagnostic counter changed.

No storage-format rewrite is presumed. It requires measured evidence that
unique byte/decode amplification remains a dominant blocker after runtime
fixes.

## Claim Boundary

Selected product-only recovery mechanisms are integrated on `main`. The
remainder of the recovered successor branch is rejected research, not an
alternate product or pending wholesale merge.

No absolute performance, comparison-viewer, release, or successor-completion
claim is active. The completed work establishes scoped correctness and product
behavior only; future performance claims still require a named workload,
hardware, metric, sampling method, and threshold.

Public-data and segmentation outcomes remain deferred and separate.
