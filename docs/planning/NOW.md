# Current Work

Last updated: 2026-07-15

## Selected Planning Checkpoint

The foundation implementation and deletion audit through WP-15 are complete.
The first selected post-foundation task is the multi-hour TIFF import and
preprocessing bottleneck.

The [import and preprocessing performance
handoff](../plans/active/IMPORT_PREPROCESSING_PERFORMANCE_HANDOFF.md) records the
read-only diagnosis, hard-cutover target, dependency-ordered work packages,
quantitative acceptance gates, risks, deletions, and verification boundary.
The owner approved implementation on 2026-07-15. IP-00 through IP-03 are
implemented: observability, source-native decode-once ingest, the six-file
checkpoint hard cutover, batched durability, encoded publication, bounded
ordered CPU work, locality-aware scientific validation, and truthful named
progress are now the sole product authorities. The staged verified capability
is transferred across atomic publication through a root-identity seal and
metadata-only currentness checks, so imported open-ready no longer repeats
exact or scientific validation. The import-specific native
product scenario and strict normal-product T5 runner required by IP-04 are also
implemented. The source boundary now admits only the audited uncompressed,
LZW, current/old Deflate, and PackBits decoder set, binds opened source file
descriptors to reviewed generation facts, and retains descriptor-bound
checkpoint files and shared worker readers.

Automated correctness, a one-session dirty-worktree release diagnostic, and
the two public supporting product scenarios on a mapped physical X display are
evidence for the implementation, not closeout. The active checkpoint is IP-04
qualification: run the complete checks from one clean immutable revision, the
five-session T2 protocol, the three-session private timing protocol, and the
private normal mapped product workflow on owner-accepted HW-2; record accepted
skips/risks and accept the final deletion audit. Neither the local HW-2 profile
nor private T5-facts digest is owner-accepted, so current timing evidence is
diagnostic only. This handoff remains active until those product and
performance gates are accepted. Other unresolved work remains in [the
backlog](../BACKLOG.md).
