# Current Work

Last updated: 2026-07-12

## Current Checkpoint

WP-10A is accepted and immutable at `foundation-wp-10a-exit-1`
(`9b3a81d79a50027c0a8ddedc535021809a99d928`). WP-09A entry work is active.

The unified runtime is the sole live interactive dataset-demand and CPU-byte
authority. Analysis execution remains unavailable until WP-12.

## WP-09A Entry Boundary

The entry must bind to tag `foundation-wp-10a-exit-1`, commit
`9b3a81d79a50027c0a8ddedc535021809a99d928`, and tree
`c57db58464c77aa1699238a8f5ee3bcc164737b0`. It will freeze an off-product
`render-wgpu` runtime, a test/reference CPU renderer, per-frame work and byte
budgets, genuine runtime-lease fixtures, and GPU evidence reporting. The
current renderer remains the sole product route until WP-09B; WP-09A may not
modify or activate product paths. Because the successor is deliberately
unreachable, affected-path product-open validation is explicitly deferred to
WP-09B; the trusted real-GPU lane remains required for WP-09A itself.

WP-10B is the protected-branch checkpoint after WP-09A, following the
[foundation handoff](../plans/active/FOUNDATION_REFACTOR_HANDOFF.md).
